name: sync-local

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: self-hosted

    steps:
     - name: Pull latest changes into local repo
        run: |
          cd C:\Users\flawy\Documents\pipeline\main\Hello
          git pull origin main

     - name: Run hello.py
        run: |
          cd C:\Users\flawy\Documents\pipeline\main\Hello
          python hello.py